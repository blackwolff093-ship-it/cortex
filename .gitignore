# dependencies
node_modules/

# build output
dist/
dist-electron/
release/

# runtime data — never publish
.cortex/
userData/
backups/
scratch/
*.db
*.db-shm
*.db-wal

# local machine config (contains absolute paths and permission grants)
.claude/settings.local.json

# os / editor
.DS_Store
*.orig
*.rej
